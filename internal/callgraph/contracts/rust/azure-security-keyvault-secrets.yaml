schema_version: "2"
ecosystem: rust
library:
  name: azure_security_keyvault_secrets
  coordinates:
    - azure_security_keyvault_secrets
  version_range: ">=0.1.0,<2.0.0"
  description: "Azure Key Vault secrets client (generated SDK)"

# API read at azure_security_keyvault_secrets 1.0.0
# (src/generated/clients/secret_client.rs). This crate performs NO cryptographic
# operation: its whole surface is get/set/delete/backup/restore of a stored
# secret value. It is contracted so a consumer's secret-handling variables
# resolve, and for no stronger claim — the matching rule reports
# related-crypto-material, never an algorithm.
contracts:
  - method: azure_security_keyvault_secrets::SecretClient.get_secret
    arity: 3
    return: { type: core::result::Result, confidence: high }
    role: operation
  - method: azure_security_keyvault_secrets::SecretClient.set_secret
    arity: 3
    return: { type: core::result::Result, confidence: high }
    role: operation
  - method: azure_security_keyvault_secrets::SecretClient.update_secret_properties
    arity: 4
    return: { type: core::result::Result, confidence: high }
    role: operation
  - method: azure_security_keyvault_secrets::SecretClient.backup_secret
    arity: 2
    return: { type: core::result::Result, confidence: high }
    role: operation
  - method: azure_security_keyvault_secrets::SecretClient.restore_secret
    arity: 2
    return: { type: core::result::Result, confidence: high }
    role: operation

hierarchy: {}
